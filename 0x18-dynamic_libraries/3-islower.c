#include "main.h"

/**
 * _islower - To checks for lowercase letters
 * @c: alphabet to check
 * Return: 1 if c is lowercase, 0 if otherwise
 */

int _islower(int c)
{
	return (c >= 'a' && c <= 'z');
}
